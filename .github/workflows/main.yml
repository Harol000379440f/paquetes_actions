name: Test Composite Actions

on: [push]

jobs:
  test-build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4.2.0
            
      - name: ls
        run: | #Operador pai
          ls -la
          cd ./.github/workflows
          ls -la
          chmod +x script.sh
          ./script.sh

          
      - name: ls
        uses: ./.github/workflows/actions/my-actions/action.yml
